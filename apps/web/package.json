{
  "name": "@ledger/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "echo \"no web tests until stage 6\" && exit 0",
    "typecheck": "echo \"no web sources until stage 6\" && exit 0"
  }
}
